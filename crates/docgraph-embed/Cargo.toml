[package]
name = "docgraph-embed"
version = "0.1.0"
edition = "2021"
description = "Local ONNX embedding engine for DocGraph (PyO3)"

[lib]
name = "docgraph_embed"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
fastembed = "4"
once_cell = "1"

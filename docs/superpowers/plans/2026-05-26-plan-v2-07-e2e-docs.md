# Plan v2-07 — Skill, E2E & Docs

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `/document` Cursor skill, end-to-end integration test, and rewrite README for v2.

**Architecture:** Skill file at `skills/document/SKILL.md`. E2E test uploads via API, indexes with mocked embedder, searches via SearchService. README documents Poetry setup, Ollama, Cursor mcp.json, and skill installation.

**Tech Stack:** pytest, markdown docs

**Depends on:** Plan v2-06  
**Blocks:** nothing (v2 complete)

**Spec refs:** §10 Cursor Skill, §13 Testing Strategy, §12 Install

---

### Task 1: `/document` Cursor Skill

**Files:**
- Create: `skills/document/SKILL.md`

- [ ] **Step 1: Write skill file**

```markdown
---
name: document
description: Query uploaded documents via BoostMCP RAG. Use when the user message starts with /document.
---

# /document — Query BoostMCP Knowledge Base

When the user's message starts with `/document`, follow this workflow exactly.

## Trigger

- Message begins with `/document` (with or without space after)
- Example: `/document How do I configure Ollama embedding?`
- Example: `/document --folder BoostMCP --tag design What changed in v2?`

## Parse Input

1. Strip the `/document` prefix
2. Extract optional filters:
   - `--folder <name>` → pass as `folder` to MCP
   - `--tag <name>` → pass as `tags: ["<name>"]` to MCP
3. Remaining text is the **query**

## Required MCP Call

Call `search_documents` on the **boostmcp** MCP server:

```
search_documents(
  query="<parsed query>",
  folder="<folder or omit>",
  tags=["<tag>"] or omit,
  top_k=5
)
```

## Answer Rules

1. Answer **only** from returned chunks — do not use outside knowledge
2. If `results` is empty or error mentions no matches:
   - Tell user: "No matching documents. Upload files at http://127.0.0.1:8080"
3. If error mentions Ollama:
   - Tell user to start Ollama and run `ollama pull nomic-embed-text`
4. Cite sources inline: `[filename, chunk N]` or `[filename, p.X]` if source_page exists
5. If chunks conflict, note the conflict and cite both sources

## Optional Follow-up

If the user needs full document text, call `get_document(doc_id=...)`.
To see what's indexed, call `list_documents`.
```

- [ ] **Step 2: Commit**

```bash
git add skills/document/SKILL.md
git commit -m "docs: add /document Cursor skill for BoostMCP RAG"
```

---

### Task 2: End-to-end integration test

**Files:**
- Create: `tests/test_e2e.py`

- [ ] **Step 1: Write E2E test**

```python
# tests/test_e2e.py
import io
import pytest
import respx
import httpx
from httpx import ASGITransport, AsyncClient

from boostmcp.config import Config
from boostmcp.mcp.search import SearchService
from boostmcp.store import ChromaStore, SQLiteStore
from boostmcp.embed.ollama import OllamaEmbedder
from boostmcp.web.app import create_app


@pytest.mark.asyncio
@respx.mock
async def test_upload_then_search_e2e(tmp_data_dir, monkeypatch):
    cfg = Config(data_dir=tmp_data_dir)
    app = create_app(cfg)

    vec = [0.2] * 768
    respx.post(f"{cfg.ollama_url}/api/embed").mock(
        return_value=httpx.Response(200, json={"embeddings": [vec]})
    )

    content = b"# BoostMCP v2\n\nDocument RAG with Ollama embedding."
    transport = ASGITransport(app=app)

    async with AsyncClient(transport=transport, base_url="http://test") as client:
        upload = await client.post(
            "/api/documents",
            files={"file": ("v2.md", io.BytesIO(content), "text/markdown")},
            data={"folder": "docs", "tags": "v2,rag"},
        )
        assert upload.status_code == 202
        doc_id = upload.json()["doc_id"]

        # Wait for background index (poll status)
        for _ in range(20):
            docs = await client.get("/api/documents")
            doc = next(d for d in docs.json() if d["id"] == doc_id)
            if doc["status"] in ("ready", "error"):
                break
            import asyncio
            await asyncio.sleep(0.1)

        assert doc["status"] == "ready"
        assert doc["chunk_count"] >= 1

    st = app.state.boostmcp
    svc = SearchService(cfg, st.sqlite, st.chroma, st.embedder)
    results = await svc.search("Ollama embedding", folder="docs")
    assert len(results) >= 1
    assert "Ollama" in results[0].text
```

- [ ] **Step 2: Run E2E test**

```bash
poetry run pytest tests/test_e2e.py -v
```

Expected: PASS

- [ ] **Step 3: Run full suite**

```bash
poetry run pytest tests/ -v
```

- [ ] **Step 4: Commit**

```bash
git add tests/test_e2e.py
git commit -m "test: add upload-to-search end-to-end integration test"
```

---

### Task 3: Rewrite README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace README content**

```markdown
# BoostMCP

Local document RAG server for Cursor via MCP. Upload files through a Web UI, index them with Ollama embeddings, and query your documents in Cursor using the `/document` skill.

## Prerequisites

- Python 3.10+
- [Poetry](https://python-poetry.org/)
- [Ollama](https://ollama.com/) running locally
- Embedding model: `ollama pull nomic-embed-text`

## Install

```bash
poetry install
poetry install -E openai   # optional: cloud embedding
ollama pull nomic-embed-text
```

## Run

```bash
poetry run boostmcp serve
```

- Web UI: http://127.0.0.1:8080 (upload and manage documents)
- MCP: stdio (Cursor attaches automatically)

## Cursor MCP Configuration

Add to Cursor MCP settings:

```json
{
  "mcpServers": {
    "boostmcp": {
      "command": "poetry",
      "args": ["run", "boostmcp", "serve"],
      "cwd": "/path/to/BoostMCP"
    }
  }
}
```

## `/document` Skill

Copy `skills/document/SKILL.md` to your Cursor skills directory:

- Global: `~/.cursor/skills/document/SKILL.md`
- Project: `.cursor/skills/document/SKILL.md`

Then in Cursor chat:

```
/document How do I configure the embedding provider?
/document --folder docs --tag v2 What is BoostMCP v2?
```

## MCP Tools

| Tool | Description |
|------|-------------|
| `search_documents` | Semantic search over indexed documents |
| `list_documents` | List documents with optional filters |
| `get_document` | Get full converted markdown for a document |

## Configuration

| Env Variable | Default | Description |
|---|---|---|
| `BOOSTMCP_DATA_DIR` | `~/.boostmcp` | Data directory |
| `BOOSTMCP_WEB_PORT` | `8080` | Web UI port |
| `BOOSTMCP_EMBED_PROVIDER` | `ollama` | `ollama` or `openai` |
| `BOOSTMCP_OLLAMA_URL` | `http://localhost:11434` | Ollama endpoint |
| `BOOSTMCP_OLLAMA_EMBED_MODEL` | `nomic-embed-text` | Embedding model |

See [v2 design spec](docs/superpowers/specs/2026-05-26-boostmcp-v2-rag-design.md) and [implementation plans](docs/superpowers/plans/2026-05-26-boostmcp-v2-index.md).

## Test

```bash
poetry run pytest tests/ -v
poetry run pytest tests/ -m integration -v   # requires Ollama
```
```

- [ ] **Step 2: Update `.gitignore` for Python**

Add to `.gitignore`:

```
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
dist/
*.egg-info/
```

- [ ] **Step 3: Commit**

```bash
git add README.md .gitignore
git commit -m "docs: rewrite README for BoostMCP v2 document RAG"
```

---

### Task 4: Final verification

- [ ] **Step 1: Full test suite**

```bash
poetry run pytest tests/ -v
```

Expected: all PASS

- [ ] **Step 2: Manual smoke checklist**

1. `poetry run boostmcp serve` — Web UI loads at :8080
2. Upload a PDF or `.md` file — status becomes `ready`
3. Cursor MCP shows `search_documents`, `list_documents`, `get_document`
4. `/document` skill returns answer with citation

- [ ] **Step 3: Tag v2 complete**

```bash
git tag -a v2.0.0-alpha -m "BoostMCP v2 document RAG MVP"
```
